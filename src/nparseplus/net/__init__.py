"""Network clients (P99 wiki, PigParse hub/API, nparse websocket sharing).

Qt-free: models and clients only. UI marshals results back to the GUI
thread itself (Qt signals emitted from worker threads are queued).
"""
